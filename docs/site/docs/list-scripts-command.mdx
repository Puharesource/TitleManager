---
id: list-scripts-command
title: List Scripts Command
---

Legacy TitleManager scripts are not supported in TitleManager Next because arbitrary script execution from configuration is disabled. The command remains as a compatibility entry point and reports that scripts are unsupported.

### Syntax

```
/tm scripts
```
