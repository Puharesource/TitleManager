---
id: list-animations-command
title: List Animations Command
---

Lists every registered animation placeholder on the running server, including built-in placeholders and `plugins/TitleManager/animations/*.txt` files.

Fresh installations include `left-to-right` and `right-to-left` from the bundled default animation files.

## Syntax

```console
/tm animations
```
