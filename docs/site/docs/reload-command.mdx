---
id: reload-command
title: Reload Command
---

Reloads TitleManager's split configuration and animation files transactionally. If validation fails, the previous running configuration remains active. Legacy script files are not loaded in TitleManager Next.

### Format

```
/tm reload
```
